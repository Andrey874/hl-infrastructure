upstream backend {
        {{ range service "web" }}
        server {{.Node}}:{{.Port}};{{end}}
}

server {
        listen 80;
        server_name {{ ansible_eth0.ipv4.address }};
        location / {
          proxy_pass http://backend;
        }
}
